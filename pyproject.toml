[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "quantlab"
version = "0.1.0"
description = "A quantitative finance research toolkit"
dependencies = [
    "numpy",
    "scipy"
]

[tool.setuptools.packages.find]
where = ["src"]